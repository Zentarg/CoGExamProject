<Page
    x:Class="MainWindow.Views.Store"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:MainWindow.Views"
    xmlns:ViewModel="using:MainWindow.ViewModels"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">

    <Page.DataContext>
        <ViewModel:StoreVM/>
    </Page.DataContext>

    <Grid>
    <TextBlock FontSize="75">Store</TextBlock>
        <Button Margin="372,355,0,0" VerticalAlignment="Top" Command="{Binding DoAddGame}">
            AddGame
        </Button>
        <Button Margin="472,355,0,0" VerticalAlignment="Top" Command="{Binding DoLoadGames}">
            LoadGames
        </Button>
    </Grid>
</Page>
