<Page
    x:Class="MainWindow.Views.Library"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:MainWindow.Views"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:viewModels="using:MainWindow.ViewModels"
    mc:Ignorable="d"
    Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">
    <Page.DataContext>
        <viewModels:GameTemplateVM />
    </Page.DataContext>

    <Page.Resources>
        <Style x:Key="TextBlockStyle" TargetType="TextBlock">
            <Setter Property="FontSize" Value="36" />
            <Setter Property="Width" Value="300" />
            <Setter Property="Margin" Value="20,20,20,20" />
        </Style>
        <Style x:Key="ButtonStyle" TargetType="Button">
            <Setter Property="FontSize" Value="24" />
            <Setter Property="Margin" Value="10,10,10,10" />
        </Style>
    </Page.Resources>

    <Grid>

        <StackPanel Orientation="Horizontal">
            <!--  List Part  -->
            <StackPanel>
                <ListView ItemsSource="{Binding GameList}" SelectedItem="{Binding SelectedGame, Mode=TwoWay}">
                    <ListView.ItemTemplate>
                        <DataTemplate>
                            <StackPanel Orientation="Horizontal">
                                <ListView
                                    x:Name="BaseExample"
                                    ItemTemplate="{StaticResource ContactListViewTemplate}"
                                     BorderThickness="1"
                                    BorderBrush="{ThemeResource SystemControlForegroundBaseMediumLowBrush}"
                                    Width="350" 
                                     Height="400"
                                    HorizontalAlignment="Left"/>


                                <TextBlock
                                    Margin="10,10,10,10"
                                    VerticalAlignment="Center"
                                    FontSize="18"
                                    Text="{Binding Name}" />
                            </StackPanel>
                        </DataTemplate>
                    </ListView.ItemTemplate>
                </ListView>

                <TextBlock Text="Game List"/>
                <TextBlock Text="Goes Here"/>
            </StackPanel>

            <!--  Details Part  -->
            <StackPanel>
                <StackPanel
                    Margin="10,10,10,10"
                    BorderBrush="DarkGray"
                    BorderThickness="3,3,3,3"
                    Orientation="Vertical">
                    <Image
                        Width="300"
                        Height="40"
                        Margin="10,10,10,10"
                        Source="{Binding SelectedGame.ThumbnailImagePath}" />

                    <TextBlock Style="{StaticResource TextBlockStyle}" Text="{Binding SelectedGame.Name}" SelectionChanged="TextBlock_SelectionChanged_1" />
                    <TextBlock Style="{StaticResource TextBlockStyle}" Text="{Binding SelectedGame.Categories}" SelectionChanged="TextBlock_SelectionChanged" />
                    <TextBlock Style="{StaticResource TextBlockStyle}" Text="{Binding SelectedGame.Description}" />

                </StackPanel>
            </StackPanel>
        </StackPanel>

    </Grid>
</Page>