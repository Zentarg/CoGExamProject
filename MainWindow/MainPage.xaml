<Page
    x:Class="MainWindow.MainPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:MainWindow"
    xmlns:viewmodel="using:MainWindow.ViewModels"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">

    <Page.DataContext>
        <viewmodel:MainPageVM/>
    </Page.DataContext>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="50"/>
            <RowDefinition Height="1*"/>
        </Grid.RowDefinitions>
        <Grid Grid.Row="0">
            <StackPanel Orientation="Horizontal">
                <Button Height="50" FontFamily="Segoe MDL2 Assets" Content="&#xE830;" FontSize="25" Width="56"/>
                <Button Height="50" FontFamily="Segoe MDL2 Assets" Content="&#xEA47;" FontSize="25" Width="56"/>
            </StackPanel>
        </Grid>
        <SplitView Grid.Row="1" IsPaneOpen="{Binding IsPaneOpen}" OpenPaneLength="256" CompactPaneLength="56" DisplayMode="CompactOverlay">
            <SplitView.Pane>
                <StackPanel>
                    <Button Command="{Binding DoTogglePane}">
                        <TextBlock FontFamily="Segoe MDL2 Assets" Text="&#xE700;" FontSize="35" Width="256"/>
                    </Button>
                    <Button>
                        <StackPanel Orientation="Horizontal">
                            <TextBlock FontFamily="Segoe MDL2 Assets" Text="&#xE719;" FontSize="35" Width="50"/>
                            <TextBlock Margin="22, 0, 0, 0" FontSize="23" Width="164">
                                Shop
                            </TextBlock>
                        </StackPanel>
                    </Button>
                    <Button>
                        <StackPanel Orientation="Horizontal">
                            <TextBlock FontFamily="Segoe MDL2 Assets" Text="&#xE7FC;" FontSize="35" Width="50"/>
                            <TextBlock Margin="22, 0, 0, 0" FontSize="23" Width="164">
                                Library
                            </TextBlock>
                        </StackPanel>
                    </Button>
                </StackPanel>
            </SplitView.Pane>
            <SplitView.Content>
                <Frame>

                </Frame>
            </SplitView.Content>
        </SplitView>
    </Grid>
</Page>
